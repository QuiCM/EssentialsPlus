using System;
using TShockAPI;

namespace Essentials
{
    //Split commands into a new file. Makes things cleaner
    public static class ECommands
    {
        public static void BackCommand(CommandArgs args)
        {
            throw new NotImplementedException();
        }
    }
}
